export enum EUserTypes {
  AMDIN = "admin",
  DEFAULT = "default",
}

export enum EPriortyTypes {
  LOW = "low",
  MED = "med",
  HIGH = "high",
}

export const headerName = "x-source";

export const s2sHeaderName = "s2s-source";
